
<h1><%= t('views.index.title')%></h1>
<h2> Esses são nossos produtos: </h2>
<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= product.id %> - <%= link_to product.name, product_path(product) %>
    </div>
    
  <% end %>
</div>
<br/>
<%= link_to "Adicione aqui o seu produto", products_new_path %>

